import { part1, part2 } from "src/days/day01";

describe("day01", () => {
  test("part1 returns 0 for empty input", () => {
    expect(part1("")).toBe(0);
  });

  test("part 2 returns 0 for empty input", () => {
    expect(part2("")).toBe(0);
  });
});
